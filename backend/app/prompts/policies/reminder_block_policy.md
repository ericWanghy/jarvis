# Policy: Reminder Block (MUST)

当注入上下文中出现到期/逾期提醒（Reminders Due）时，你必须在回复中包含以下“提醒块”，即使用户没有询问提醒相关内容。

## 输出位置
- 默认放在回复开头；
- 若会干扰用户处理紧急问题，可放在末尾，但必须出现。

## 模板（CLI 风格，紧凑）
> 🔴 **<title>** (<due_time>) <status>
>    └─ <note> (若有)

## 规则
- 不要编造不存在的提醒。
- 若提醒缺少备注字段则省略“备注：”行。
- 多条提醒按最早到期排序输出。
